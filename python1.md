 回归分析作业

 一、代数证明题

 1. 残差和为零

设一元线性回归模型为：

y_i=\beta_0+\beta_1x_i+e_i

其中残差为：

e_i=y_i-\hat{\beta}_0-\hat{\beta}_1x_i

采用最小二乘法，使残差平方和

RSS=\sum_{i=1}^{n}(y_i-\beta_0-\beta_1x_i)^2

达到最小。

对 $\beta_0$ 求偏导：

$$
\frac{\partial RSS}{\partial\beta_0}
=
-2\sum_{i=1}^{n}(y_i-\beta_0-\beta_1x_i)
$$

在最小值处令偏导数为 0：

$$
\sum_{i=1}^{n}(y_i-\hat{\beta}_0-\hat{\beta}_1x_i)=0
$$

由于括号中的部分就是残差 $e_i$，因此：

$$
\boxed{\sum_{i=1}^{n}e_i=0}
$$

 2. 残差与自变量乘积之和为零

对 $\beta_1$ 求偏导：

$$
\frac{\partial RSS}{\partial\beta_1}
=
-2\sum_{i=1}^{n}x_i(y_i-\beta_0-\beta_1x_i)
$$

在最小值处令偏导数为 0：

$$
\sum_{i=1}^{n}x_i(y_i-\hat{\beta}_0-\hat{\beta}_1x_i)=0
$$

由于括号中的部分就是残差 $e_i$，因此：

$$
\boxed{\sum_{i=1}^{n}x_ie_i=0}
$$

因此，一元线性回归的最小二乘估计满足：

$$
\sum_{i=1}^{n}e_i=0
$$

以及

$$
\sum_{i=1}^{n}x_ie_i=0
$$
## 二、理解思考题

### 1. 为什么加入一个无关特征后，R²不会下降？

普通的决定系数为：

$$
R^2=1-\frac{RSS}{TSS}
$$

当模型中加入一个新的变量时，最小二乘法会重新寻找一组参数，
所以加入变量后 RSS 不会比原来的 RSS 更大，即：

$$
RSS_{new}\leq RSS_{old}
$$

而 TSS 不发生变化，因此 R² 不会下降，只可能上升或者保持不变。

即使加入的变量是随机生成的，它也可能在当前样本中产生一些相关性，
从而让 RSS 出现一点下降，所以 R² 还是可能增加。

### 2. 为什么调整后的 R² 可能下降？

调整后的 R² 在计算时考虑了自由度：

$$
R^2_{adj}
=
1-\frac{RSS/(n-p-1)}{TSS/(n-1)}
$$

增加一个自变量以后，虽然 RSS 一般不会增加，
但是模型使用的参数变多了，残差自由度会减少。

如果新加入的变量本身和 Sales 没有什么实际关系，
它带来的 RSS 降低很小，但是自由度却减少了，
所以经过调整以后，R²_adj 反而可能下降。

因此，普通 R² 比较容易随着变量增加而增加，
而调整后的 R² 会对增加变量进行一定的惩罚。